name: PR Workflow Notice (not enforcement)

on:
  pull_request:
    branches: [ main ]

jobs:
  informational-only:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Code
        uses: actions/checkout@v4

      - name: Run Audit Checks
        run: |
          echo "Informational only. This workflow does not enforce branch protection."
          # Cài đặt điều kiện kiểm tra ở đây (VD: linter, test)
